version: '3.8'
services:
  productor-python:
    build: ../Productores/productor-python
    container_name: productor-python
    environment:
      - RABBITMQ_HOST=10.6.101.161
    networks:
      - taller_net

  consumidor-python:
    build: ../Consumidores/consumidor-python
    container_name: consumidor-python
    environment:
      - RABBITMQ_HOST=10.6.101.161
    networks:
      - taller_net

networks:
  taller_net:
    driver: bridge
